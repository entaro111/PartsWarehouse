@model IEnumerable<PartsWarehouse.Models.MPK>

@{
    ViewBag.Title = "Miejsca powstawania kosztów";
}

<h2>Miejsca powstawania kosztów</h2>

<p>
    @Html.ActionLink("Dodaj nowe miejsce", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa)
        </td>
        <td>
            @Html.ActionLink("Edycja", "Edit", new { id=item.Id_MPK }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.Id_MPK }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.Id_MPK })
        </td>
    </tr>
}

</table>
